import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains:['dayve.vn', 'encrypted-tbn0.gstatic.com', 'pantravel.vn', 'localhost']
  }
};

export default nextConfig;
